package section3;

public class MemoryManagement {
    //Thread run in a shared memory space while process run in a separate memory space
    //Thread has its own stack memory but all thread share the same heap memory
    //Synchronization is sharing of resources without interference using mutual exclusion
}
