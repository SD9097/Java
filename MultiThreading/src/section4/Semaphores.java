package section4;

public class Semaphores {
    //semaphores are simple variables(abstract data type) that can be used to control access to a shared resource
    //It is a record of how many units of a particular resource are available. We have to wait until a unit of resource become available again

    //Counting semaphores: allows an arbitrary resource count
    //Binary semaphores: allows only 0 or 1
}
